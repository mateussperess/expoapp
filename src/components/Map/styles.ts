import { StyleSheet } from "react-native";

export const styleMap = StyleSheet.create({
  mapContainer: {
    width: "100%",
    height: 400,
    borderRadius: 15,
    marginTop: 16,
    marginBottom: 16,
  },
  map: {
    flex: 1,
  },
});
