import { StyleSheet } from "react-native";

const easterStyle = StyleSheet.create({
  container: {
    display: "flex",
    flex: 1,
    width: "100%",
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: "#333",
  },
  text: {
    fontSize: 15,
    width: "80%",
    textAlign: "center",
    color: "#fff",
    fontWeight: "bold",
  },
});

export default easterStyle;
